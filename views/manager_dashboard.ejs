<!DOCTYPE html>
<html>
<head>
  <title>Manager Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <img src="/image/logo.png" alt="Logo" class="logo" style="height:40px;vertical-align:left;margin-right:500px;">
    <% if (!userId) { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } else { %>
      <a href="/logout">Logout</a>
    <% } %>
  </nav>
  <div class="container" style="display:flex; flex-direction:column; align-items:center; justify-content:center; min-height:70vh;">
    <div class="auth-card" style="max-width:500px;">
      <h1 class="auth-title">Welcome to the Manager Dashboard</h1>
      <p style="text-align:center; margin-bottom:2em;">Only accessible to users with the <b>manager</b> role.</p>
      <a href="/expenses" class="btn-primary" style="text-align:center;">View All Expenses</a>
    </div>
  </div>
  <% /* Footer partial (if available) */ %>
  <%- typeof include === 'function' ? include('partials/footer') : '' %>
</body>
</html>
